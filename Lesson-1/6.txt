$x = true xor true;
var_dump($x);

Результат 'true' объясняется тем, что $x было присвоено значение 'true', а остальное игнорируется. Если выражение поставить в скобки
(true xor true), то $x будет присвоено значение 'false' 
